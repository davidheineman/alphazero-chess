network:
  res_blocks: 6
  channels: 96

mcts:
  simulations: 400
  batch_size: 64

self_play:
  games: 40
  max_moves: 120
  parallel_games: 10
  num_workers: 12

train:
  epochs: 5
  batch_size: 512

eval:
  games: 4
  simulations: 200

run:
  iterations: 40

stockfish:
  games: 4
  max_skill: 5
  every_n_iters: 10

pretrain:
  max_positions: 300000
  epochs: 3
