network:
  res_blocks: 6
  channels: 96

mcts:
  simulations: 50
  batch_size: 32

self_play:
  games: 20
  max_moves: 100
  parallel_games: 5
  num_workers: 8

train:
  epochs: 8
  batch_size: 512

eval:
  games: 4
  simulations: 25

run:
  iterations: 40

stockfish:
  games: 4
  skill_level: 0
  every_n_iters: 10

pretrain:
  max_positions: 300000
  epochs: 3
